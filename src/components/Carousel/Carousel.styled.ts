import styled from 'styled-components'
import tw from 'twin.macro'

export const SDiv = styled.div`
    ${tw`h-[600px] bg-white`}
`
export const SGradient = styled.div`
    ${tw`h-[50%] bg-gradient-to-b  from-stone-900`}
`
